---
type: tool_used
tool: mcp__sortwell__capture
min: 3
max: 6
---

The transcript holds four distinct things: a pleasantry, a decision, a task,
and a project update. One `capture` call files one item, so filing the block
in a single call loses three of them.

At least three calls means the agent split it. More than six means it split
mid-sentence. Both failures are worth seeing.
